import React from 'react'
import {
    Typography
} from '@mui/material'

function LoadingScreen() {
    return (
            <Typography variant='subtitle2' sx={{width: '100%', height: '100%'}}>Loading...</Typography>
  )
}

export default LoadingScreen